# Host sealed model tools

OpenSSL 3.x EVPを使うmicroT-FS sealed model package v1のHost専用reference implementationです。

build、CLI、key取扱い、test vector、security limitationは
[`../../docs/security/host-sealed-model-tools.md`](../../docs/security/host-sealed-model-tools.md)を参照してください。

`src/`のembedded production codeやRA/ST target projectからは参照されません。
